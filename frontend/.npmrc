# Pin the official registry for this project. The user-level ~/.npmrc points
# at a China mirror (npmmirror), which today produced (a) a lockfile missing
# linux-side optional deps -> npm ci EUSAGE in CI, and (b) npm publish needing
# an explicit --registry. Project-level override kills that class of failures.
registry=https://registry.npmjs.org
